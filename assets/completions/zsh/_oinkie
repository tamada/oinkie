#compdef oinkie

autoload -U is-at-least

_oinkie() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'-l+[Log level for the application]:LEVEL:(error warn info debug trace off)' \
'--level=[Log level for the application]:LEVEL:(error warn info debug trace off)' \
'-h[Print help]' \
'--help[Print help]' \
'-V[Print version]' \
'--version[Print version]' \
":: :_oinkie_commands" \
"*::: :->oinkie" \
&& ret=0
    case $state in
    (oinkie)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:oinkie-command-$line[1]:"
        case $line[1] in
            (info)
_arguments "${_arguments_options[@]}" : \
'-h[Print help]' \
'--help[Print help]' \
&& ret=0
;;
(lift)
_arguments "${_arguments_options[@]}" : \
'-d+[Specify the directory for putting the resultant JSON files of the lifted programs (default\: '\''./pcodes'\'' directory)]:DIRECTORY:_files' \
'--dest=[Specify the directory for putting the resultant JSON files of the lifted programs (default\: '\''./pcodes'\'' directory)]:DIRECTORY:_files' \
'-l+[Specify the lifter type]:LIFTER_TYPE:(ghidra angr ida-pro binary-ninja)' \
'--lifter-type=[Specify the lifter type]:LIFTER_TYPE:(ghidra angr ida-pro binary-ninja)' \
'-H+[Path to the lifter'\''s installation directory. If not specified, the lifter'\''s own environment variable (GHIDRA_HOME for Ghidra) is read, then the usual install locations are searched. The error names which variable to set.]:HOME:_files' \
'--home=[Path to the lifter'\''s installation directory. If not specified, the lifter'\''s own environment variable (GHIDRA_HOME for Ghidra) is read, then the usual install locations are searched. The error names which variable to set.]:HOME:_files' \
'-i+[Directory for the lifter to work in, kept rather than discarded. Every lifter runs in one, since that is where its script writes; Ghidra also keeps its project there. If not specified, a temporary directory is used and deleted.]:DIRECTORY:_files' \
'--intermediate=[Directory for the lifter to work in, kept rather than discarded. Every lifter runs in one, since that is where its script writes; Ghidra also keeps its project there. If not specified, a temporary directory is used and deleted.]:DIRECTORY:_files' \
'--script=[Path to a custom lifting script, replacing the built-in one. The language is the lifter'\''s own\: Java for Ghidra. It must write {input file name}.json into its working directory.]:SCRIPT:_files' \
'-j+[Lift up to N files at a time (default\: 1, one after another). Lifting runs a whole decompiler process per file, and several of them against a Ghidra installation whose language cache has not been built yet can corrupt it, so parallelism is opt-in.]:N:_default' \
'--jobs=[Lift up to N files at a time (default\: 1, one after another). Lifting runs a whole decompiler process per file, and several of them against a Ghidra installation whose language cache has not been built yet can corrupt it, so parallelism is opt-in.]:N:_default' \
'-S[Skip if the resultant JSON file already exists]' \
'--skip[Skip if the resultant JSON file already exists]' \
'-h[Print help]' \
'--help[Print help]' \
'*::files -- Path to the binary or intermediate files to lift:_files' \
&& ret=0
;;
(extract)
_arguments "${_arguments_options[@]}" : \
'-d+[Specify the directory for putting the resultant JSON files for the extracted birthmarks (default\: '\''./birthmarks'\'' directory)]:DIRECTORY:_files' \
'--dest=[Specify the directory for putting the resultant JSON files for the extracted birthmarks (default\: '\''./birthmarks'\'' directory)]:DIRECTORY:_files' \
'-b+[Type of birthmark to extract. fc (Function Calls) and op (Opcode) with set, seq, and freq variants are supported. For example, '\''op-seq'\'' extracts the sequence of operations as a birthmark, while '\''fc-freq'\'' extracts the frequency of function calls. k-grams are written with the k in the name\: '\''op-3gram-set'\''. Any k parses, not only the ones '\''oinkie info'\'' lists. The full birthmark types can be found by running '\''oinkie info'\''.]:BIRTHMARK_TYPE:((fc-seq\:"the sequence of method calls in a program"
fc-freq\:"the frequency of method calls in a program"
fc-set\:"the set of method calls in a program"
op-seq\:"the sequence of operations in a program"
op-set\:"the set of operations in a program"
op-freq\:"the frequency of operations in a program"
op-1gram-seq\:"the sequence of 1-grams of operations in a program"
op-2gram-seq\:"the sequence of 2-grams of operations in a program"
op-3gram-seq\:"the sequence of 3-grams of operations in a program"
op-4gram-seq\:"the sequence of 4-grams of operations in a program"
op-5gram-seq\:"the sequence of 5-grams of operations in a program"
op-6gram-seq\:"the sequence of 6-grams of operations in a program"
op-7gram-seq\:"the sequence of 7-grams of operations in a program"
op-8gram-seq\:"the sequence of 8-grams of operations in a program"
op-1gram-freq\:"the frequency of 1-grams of operations in a program"
op-2gram-freq\:"the frequency of 2-grams of operations in a program"
op-3gram-freq\:"the frequency of 3-grams of operations in a program"
op-4gram-freq\:"the frequency of 4-grams of operations in a program"
op-5gram-freq\:"the frequency of 5-grams of operations in a program"
op-6gram-freq\:"the frequency of 6-grams of operations in a program"
op-7gram-freq\:"the frequency of 7-grams of operations in a program"
op-8gram-freq\:"the frequency of 8-grams of operations in a program"
op-1gram-set\:"the set of 1-grams of operations in a program"
op-2gram-set\:"the set of 2-grams of operations in a program"
op-3gram-set\:"the set of 3-grams of operations in a program"
op-4gram-set\:"the set of 4-grams of operations in a program"
op-5gram-set\:"the set of 5-grams of operations in a program"
op-6gram-set\:"the set of 6-grams of operations in a program"
op-7gram-set\:"the set of 7-grams of operations in a program"
op-8gram-set\:"the set of 8-grams of operations in a program"))' \
'--birthmark-type=[Type of birthmark to extract. fc (Function Calls) and op (Opcode) with set, seq, and freq variants are supported. For example, '\''op-seq'\'' extracts the sequence of operations as a birthmark, while '\''fc-freq'\'' extracts the frequency of function calls. k-grams are written with the k in the name\: '\''op-3gram-set'\''. Any k parses, not only the ones '\''oinkie info'\'' lists. The full birthmark types can be found by running '\''oinkie info'\''.]:BIRTHMARK_TYPE:((fc-seq\:"the sequence of method calls in a program"
fc-freq\:"the frequency of method calls in a program"
fc-set\:"the set of method calls in a program"
op-seq\:"the sequence of operations in a program"
op-set\:"the set of operations in a program"
op-freq\:"the frequency of operations in a program"
op-1gram-seq\:"the sequence of 1-grams of operations in a program"
op-2gram-seq\:"the sequence of 2-grams of operations in a program"
op-3gram-seq\:"the sequence of 3-grams of operations in a program"
op-4gram-seq\:"the sequence of 4-grams of operations in a program"
op-5gram-seq\:"the sequence of 5-grams of operations in a program"
op-6gram-seq\:"the sequence of 6-grams of operations in a program"
op-7gram-seq\:"the sequence of 7-grams of operations in a program"
op-8gram-seq\:"the sequence of 8-grams of operations in a program"
op-1gram-freq\:"the frequency of 1-grams of operations in a program"
op-2gram-freq\:"the frequency of 2-grams of operations in a program"
op-3gram-freq\:"the frequency of 3-grams of operations in a program"
op-4gram-freq\:"the frequency of 4-grams of operations in a program"
op-5gram-freq\:"the frequency of 5-grams of operations in a program"
op-6gram-freq\:"the frequency of 6-grams of operations in a program"
op-7gram-freq\:"the frequency of 7-grams of operations in a program"
op-8gram-freq\:"the frequency of 8-grams of operations in a program"
op-1gram-set\:"the set of 1-grams of operations in a program"
op-2gram-set\:"the set of 2-grams of operations in a program"
op-3gram-set\:"the set of 3-grams of operations in a program"
op-4gram-set\:"the set of 4-grams of operations in a program"
op-5gram-set\:"the set of 5-grams of operations in a program"
op-6gram-set\:"the set of 6-grams of operations in a program"
op-7gram-set\:"the set of 7-grams of operations in a program"
op-8gram-set\:"the set of 8-grams of operations in a program"))' \
'-S[Skip the resultant birthmark file is already exists]' \
'--skip[Skip the resultant birthmark file is already exists]' \
'-h[Print help]' \
'--help[Print help]' \
'*::files -- Path to the JSON files to extract birthmarks from:_files' \
&& ret=0
;;
(compare)
_arguments "${_arguments_options[@]}" : \
'-a+[Specify the similarity calculation algorithm.]:ALGORITHM:((cosine\:"Cosine similarity based on term frequency vectors. Available\: seq and freq"
dice\:"Dice coefficient. Available\: seq, set and freq"
euclidean\:"Euclidean distance between term frequency vectors. Available\: seq and freq"
jaccard\:"Jaccard index. Available\: seq, set and freq"
levenshtein\:"Levenshtein distance. Available\: seq"
lcs\:"Longest Common Subsequence (LCS). Available\: seq"
simpson\:"Simpson'\''s coefficient. Available\: seq, set and freq"
weighted-jaccard\:"Weighted Jaccard index based on term frequency vectors. Available\: seq and freq"))' \
'--algorithm=[Specify the similarity calculation algorithm.]:ALGORITHM:((cosine\:"Cosine similarity based on term frequency vectors. Available\: seq and freq"
dice\:"Dice coefficient. Available\: seq, set and freq"
euclidean\:"Euclidean distance between term frequency vectors. Available\: seq and freq"
jaccard\:"Jaccard index. Available\: seq, set and freq"
levenshtein\:"Levenshtein distance. Available\: seq"
lcs\:"Longest Common Subsequence (LCS). Available\: seq"
simpson\:"Simpson'\''s coefficient. Available\: seq, set and freq"
weighted-jaccard\:"Weighted Jaccard index based on term frequency vectors. Available\: seq and freq"))' \
'-A+[Specify the aggregator for combining element-wise similarity scores into a birthmark-wise similarity score. Available\: - hungarian  Use the Hungarian algorithm to find the optimal matching between elements of two birthmarks,              maximizing the total similarity score. - topn\:N     For each element in the first birthmark, consider only the top N most similar elements in the              second birthmark when calculating the overall similarity score. This can reduce noise from less              relevant matches and focus on the most significant similarities.]:METHOD:_default' \
'--aggregator=[Specify the aggregator for combining element-wise similarity scores into a birthmark-wise similarity score. Available\: - hungarian  Use the Hungarian algorithm to find the optimal matching between elements of two birthmarks,              maximizing the total similarity score. - topn\:N     For each element in the first birthmark, consider only the top N most similar elements in the              second birthmark when calculating the overall similarity score. This can reduce noise from less              relevant matches and focus on the most significant similarities.]:METHOD:_default' \
'-s+[Specify the pairing strategy for comparing files.]:STRATEGY:((all-and-self\:"All possible combinations including self-comparisons (\$_nC_2 + n\$). Used for full matrix visualization or comprehensive heatmaps"
all\:"Compares all possible combinations (\$_nC_2\$). Used for comprehensive validation of accuracy (False Positive / True Positive)"
self-coverage\:"Compares each file with itself (\$n\$). Used for sanity checks to ensure identical files yield a similarity score of 1.0"
adjacent\:"Compares only adjacent pairs in the list (\$n-1\$). Useful for comparing sequential versions (e.g., v1.0 vs v1.1, v1.1 vs v1.2)"
first-vs-others\:"Compares a specific reference file against all other files (\$n-1\$). Compares first item and all other items. Useful for comparing a baseline version against multiple variants"
last-vs-others\:"Compares a specific reference file against all other files (\$n-1\$). Compares the last item and all other items. Useful for comparing a baseline version against multiple variants"))' \
'--strategy=[Specify the pairing strategy for comparing files.]:STRATEGY:((all-and-self\:"All possible combinations including self-comparisons (\$_nC_2 + n\$). Used for full matrix visualization or comprehensive heatmaps"
all\:"Compares all possible combinations (\$_nC_2\$). Used for comprehensive validation of accuracy (False Positive / True Positive)"
self-coverage\:"Compares each file with itself (\$n\$). Used for sanity checks to ensure identical files yield a similarity score of 1.0"
adjacent\:"Compares only adjacent pairs in the list (\$n-1\$). Useful for comparing sequential versions (e.g., v1.0 vs v1.1, v1.1 vs v1.2)"
first-vs-others\:"Compares a specific reference file against all other files (\$n-1\$). Compares first item and all other items. Useful for comparing a baseline version against multiple variants"
last-vs-others\:"Compares a specific reference file against all other files (\$n-1\$). Compares the last item and all other items. Useful for comparing a baseline version against multiple variants"))' \
'-d+[Specify the destination directory for the comparing results]:DIRECTORY:_files' \
'--dest=[Specify the destination directory for the comparing results]:DIRECTORY:_files' \
'-S[Skip if the similarity file already exists for the pair of birthmarks]' \
'--skip[Skip if the similarity file already exists for the pair of birthmarks]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'*::files -- Path to the birthmark JSON files to compare:_files' \
&& ret=0
;;
(reaggregate)
_arguments "${_arguments_options[@]}" : \
'-A+[Specify the aggregator for combining element-wise similarity scores into a birthmark-wise similarity score. Available\: - hungarian  Use the Hungarian algorithm to find the optimal matching between elements of two birthmarks,              maximizing the total similarity score. - topn\:N     For each element in the first birthmark, consider only the top N most similar elements in the              second birthmark when calculating the overall similarity score. This can reduce noise from less              relevant matches and focus on the most significant similarities.]:METHOD:_default' \
'--aggregator=[Specify the aggregator for combining element-wise similarity scores into a birthmark-wise similarity score. Available\: - hungarian  Use the Hungarian algorithm to find the optimal matching between elements of two birthmarks,              maximizing the total similarity score. - topn\:N     For each element in the first birthmark, consider only the top N most similar elements in the              second birthmark when calculating the overall similarity score. This can reduce noise from less              relevant matches and focus on the most significant similarities.]:METHOD:_default' \
'-d+[Specify the result CSV file of the comparing results to reaggregate. The file contains the birthmark-wise similarity score list.]:RESULT.CSV:_files' \
'--dest-file=[Specify the result CSV file of the comparing results to reaggregate. The file contains the birthmark-wise similarity score list.]:RESULT.CSV:_files' \
'-h[Print help]' \
'--help[Print help]' \
':score_directory -- Path to the directory containing the element-wise similarity scores:_files' \
&& ret=0
;;
(run)
_arguments "${_arguments_options[@]}" : \
'-a+[Analysis to run, as '\''{birthmark}-{algorithm}'\'' -- for example '\''op-set-jaccard'\'' or '\''op-3gram-freq-cosine'\''. Run '\''oinkie info'\'' for the birthmarks and the algorithms they pair with. Any k parses in a k-gram name, not only the ones listed.]:ANALYSIS:(fc-seq-levenshtein fc-seq-lcs fc-freq-cosine fc-freq-euclidean fc-freq-weightedjaccard fc-set-dice fc-set-jaccard fc-set-simpson op-seq-levenshtein op-seq-lcs op-set-dice op-set-jaccard op-set-simpson op-freq-cosine op-freq-euclidean op-freq-weightedjaccard op-1gram-seq-levenshtein op-1gram-seq-lcs op-2gram-seq-levenshtein op-2gram-seq-lcs op-3gram-seq-levenshtein op-3gram-seq-lcs op-4gram-seq-levenshtein op-4gram-seq-lcs op-5gram-seq-levenshtein op-5gram-seq-lcs op-6gram-seq-levenshtein op-6gram-seq-lcs op-7gram-seq-levenshtein op-7gram-seq-lcs op-8gram-seq-levenshtein op-8gram-seq-lcs op-1gram-freq-cosine op-1gram-freq-euclidean op-1gram-freq-weightedjaccard op-2gram-freq-cosine op-2gram-freq-euclidean op-2gram-freq-weightedjaccard op-3gram-freq-cosine op-3gram-freq-euclidean op-3gram-freq-weightedjaccard op-4gram-freq-cosine op-4gram-freq-euclidean op-4gram-freq-weightedjaccard op-5gram-freq-cosine op-5gram-freq-euclidean op-5gram-freq-weightedjaccard op-6gram-freq-cosine op-6gram-freq-euclidean op-6gram-freq-weightedjaccard op-7gram-freq-cosine op-7gram-freq-euclidean op-7gram-freq-weightedjaccard op-8gram-freq-cosine op-8gram-freq-euclidean op-8gram-freq-weightedjaccard op-1gram-set-dice op-1gram-set-jaccard op-1gram-set-simpson op-2gram-set-dice op-2gram-set-jaccard op-2gram-set-simpson op-3gram-set-dice op-3gram-set-jaccard op-3gram-set-simpson op-4gram-set-dice op-4gram-set-jaccard op-4gram-set-simpson op-5gram-set-dice op-5gram-set-jaccard op-5gram-set-simpson op-6gram-set-dice op-6gram-set-jaccard op-6gram-set-simpson op-7gram-set-dice op-7gram-set-jaccard op-7gram-set-simpson op-8gram-set-dice op-8gram-set-jaccard op-8gram-set-simpson)' \
'--analysis=[Analysis to run, as '\''{birthmark}-{algorithm}'\'' -- for example '\''op-set-jaccard'\'' or '\''op-3gram-freq-cosine'\''. Run '\''oinkie info'\'' for the birthmarks and the algorithms they pair with. Any k parses in a k-gram name, not only the ones listed.]:ANALYSIS:(fc-seq-levenshtein fc-seq-lcs fc-freq-cosine fc-freq-euclidean fc-freq-weightedjaccard fc-set-dice fc-set-jaccard fc-set-simpson op-seq-levenshtein op-seq-lcs op-set-dice op-set-jaccard op-set-simpson op-freq-cosine op-freq-euclidean op-freq-weightedjaccard op-1gram-seq-levenshtein op-1gram-seq-lcs op-2gram-seq-levenshtein op-2gram-seq-lcs op-3gram-seq-levenshtein op-3gram-seq-lcs op-4gram-seq-levenshtein op-4gram-seq-lcs op-5gram-seq-levenshtein op-5gram-seq-lcs op-6gram-seq-levenshtein op-6gram-seq-lcs op-7gram-seq-levenshtein op-7gram-seq-lcs op-8gram-seq-levenshtein op-8gram-seq-lcs op-1gram-freq-cosine op-1gram-freq-euclidean op-1gram-freq-weightedjaccard op-2gram-freq-cosine op-2gram-freq-euclidean op-2gram-freq-weightedjaccard op-3gram-freq-cosine op-3gram-freq-euclidean op-3gram-freq-weightedjaccard op-4gram-freq-cosine op-4gram-freq-euclidean op-4gram-freq-weightedjaccard op-5gram-freq-cosine op-5gram-freq-euclidean op-5gram-freq-weightedjaccard op-6gram-freq-cosine op-6gram-freq-euclidean op-6gram-freq-weightedjaccard op-7gram-freq-cosine op-7gram-freq-euclidean op-7gram-freq-weightedjaccard op-8gram-freq-cosine op-8gram-freq-euclidean op-8gram-freq-weightedjaccard op-1gram-set-dice op-1gram-set-jaccard op-1gram-set-simpson op-2gram-set-dice op-2gram-set-jaccard op-2gram-set-simpson op-3gram-set-dice op-3gram-set-jaccard op-3gram-set-simpson op-4gram-set-dice op-4gram-set-jaccard op-4gram-set-simpson op-5gram-set-dice op-5gram-set-jaccard op-5gram-set-simpson op-6gram-set-dice op-6gram-set-jaccard op-6gram-set-simpson op-7gram-set-dice op-7gram-set-jaccard op-7gram-set-simpson op-8gram-set-dice op-8gram-set-jaccard op-8gram-set-simpson)' \
'-s+[Pairing strategy for file comparisons]:STRATEGY:((all-and-self\:"All possible combinations including self-comparisons (\$_nC_2 + n\$). Used for full matrix visualization or comprehensive heatmaps"
all\:"Compares all possible combinations (\$_nC_2\$). Used for comprehensive validation of accuracy (False Positive / True Positive)"
self-coverage\:"Compares each file with itself (\$n\$). Used for sanity checks to ensure identical files yield a similarity score of 1.0"
adjacent\:"Compares only adjacent pairs in the list (\$n-1\$). Useful for comparing sequential versions (e.g., v1.0 vs v1.1, v1.1 vs v1.2)"
first-vs-others\:"Compares a specific reference file against all other files (\$n-1\$). Compares first item and all other items. Useful for comparing a baseline version against multiple variants"
last-vs-others\:"Compares a specific reference file against all other files (\$n-1\$). Compares the last item and all other items. Useful for comparing a baseline version against multiple variants"))' \
'--strategy=[Pairing strategy for file comparisons]:STRATEGY:((all-and-self\:"All possible combinations including self-comparisons (\$_nC_2 + n\$). Used for full matrix visualization or comprehensive heatmaps"
all\:"Compares all possible combinations (\$_nC_2\$). Used for comprehensive validation of accuracy (False Positive / True Positive)"
self-coverage\:"Compares each file with itself (\$n\$). Used for sanity checks to ensure identical files yield a similarity score of 1.0"
adjacent\:"Compares only adjacent pairs in the list (\$n-1\$). Useful for comparing sequential versions (e.g., v1.0 vs v1.1, v1.1 vs v1.2)"
first-vs-others\:"Compares a specific reference file against all other files (\$n-1\$). Compares first item and all other items. Useful for comparing a baseline version against multiple variants"
last-vs-others\:"Compares a specific reference file against all other files (\$n-1\$). Compares the last item and all other items. Useful for comparing a baseline version against multiple variants"))' \
'-d+[Destination path for the output CSV file (default\: '\''similarities'\'' directory]:DEST:_files' \
'--dest=[Destination path for the output CSV file (default\: '\''similarities'\'' directory]:DEST:_files' \
'-A+[Specify the aggregator for combining element-wise similarity scores into a birthmark-wise similarity score. Available\: - hungarian  Use the Hungarian algorithm to find the optimal matching between elements of two birthmarks,              maximizing the total similarity score. - topn\:N     For each element in the first birthmark, consider only the top N most similar elements in the              second birthmark when calculating the overall similarity score. This can reduce noise from less              relevant matches and focus on the most significant similarities. available topn\:N or topn\:all (same as topn).]:METHOD:_default' \
'--aggregator=[Specify the aggregator for combining element-wise similarity scores into a birthmark-wise similarity score. Available\: - hungarian  Use the Hungarian algorithm to find the optimal matching between elements of two birthmarks,              maximizing the total similarity score. - topn\:N     For each element in the first birthmark, consider only the top N most similar elements in the              second birthmark when calculating the overall similarity score. This can reduce noise from less              relevant matches and focus on the most significant similarities. available topn\:N or topn\:all (same as topn).]:METHOD:_default' \
'-S[Skip if the similarity file already exists for the pair of birthmarks]' \
'--skip[Skip if the similarity file already exists for the pair of birthmarks]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'*::files -- Path to the JSON files:_files' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_oinkie__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:oinkie-help-command-$line[1]:"
        case $line[1] in
            (info)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(lift)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(extract)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(compare)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(reaggregate)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(run)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_oinkie_commands] )) ||
_oinkie_commands() {
    local commands; commands=(
'info:Display information about the application' \
'lift:Lift binary files to JSON files of an intermediate representation, using a specified lifter' \
'extract:Extract birthmarks from a lifted binary file (JSON format)' \
'compare:Compare birthmarks and output the similarity score' \
'reaggregate:Reaggregate the element-wise similarity scores and recalculate the birthmark-wise similarity score' \
'run:Extract birthmarks and compare them in one command' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'oinkie commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__compare_commands] )) ||
_oinkie__subcmd__compare_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie compare commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__extract_commands] )) ||
_oinkie__subcmd__extract_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie extract commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__help_commands] )) ||
_oinkie__subcmd__help_commands() {
    local commands; commands=(
'info:Display information about the application' \
'lift:Lift binary files to JSON files of an intermediate representation, using a specified lifter' \
'extract:Extract birthmarks from a lifted binary file (JSON format)' \
'compare:Compare birthmarks and output the similarity score' \
'reaggregate:Reaggregate the element-wise similarity scores and recalculate the birthmark-wise similarity score' \
'run:Extract birthmarks and compare them in one command' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'oinkie help commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__help__subcmd__compare_commands] )) ||
_oinkie__subcmd__help__subcmd__compare_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie help compare commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__help__subcmd__extract_commands] )) ||
_oinkie__subcmd__help__subcmd__extract_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie help extract commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__help__subcmd__help_commands] )) ||
_oinkie__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie help help commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__help__subcmd__info_commands] )) ||
_oinkie__subcmd__help__subcmd__info_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie help info commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__help__subcmd__lift_commands] )) ||
_oinkie__subcmd__help__subcmd__lift_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie help lift commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__help__subcmd__reaggregate_commands] )) ||
_oinkie__subcmd__help__subcmd__reaggregate_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie help reaggregate commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__help__subcmd__run_commands] )) ||
_oinkie__subcmd__help__subcmd__run_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie help run commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__info_commands] )) ||
_oinkie__subcmd__info_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie info commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__lift_commands] )) ||
_oinkie__subcmd__lift_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie lift commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__reaggregate_commands] )) ||
_oinkie__subcmd__reaggregate_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie reaggregate commands' commands "$@"
}
(( $+functions[_oinkie__subcmd__run_commands] )) ||
_oinkie__subcmd__run_commands() {
    local commands; commands=()
    _describe -t commands 'oinkie run commands' commands "$@"
}

if [ "$funcstack[1]" = "_oinkie" ]; then
    _oinkie "$@"
else
    compdef _oinkie oinkie
fi
